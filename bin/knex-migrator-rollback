#!/usr/bin/env node
'use strict';

const command = require('../lib/command');
const logging = require('../lib/logging');

command(
    program => program.option('--force'),
    (knexMigrator, args) => knexMigrator.rollback({
        force: args.force
    }).then(() => {
        logging.info('Rollback was successful.');
    })
);
