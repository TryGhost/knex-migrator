#!/usr/bin/env node
'use strict';

const command = require('../lib/command');
const logging = require('../lib/logging');

command(
    program => program.option('--force'),
    (knexMigrator, args) => knexMigrator.reset({
        force: args.force
    }).then(() => {
        logging.info('Finished database reset!');
    })
);
