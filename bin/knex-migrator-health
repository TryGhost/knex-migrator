#!/usr/bin/env node
'use strict';

const logging = require('../lib/logging');
const command = require('../lib/command');

command(
    () => {},
    knexMigrator => knexMigrator.isDatabaseOK().then(() => {
        logging.info('Woohoo, Database is healthy');
    })
);
